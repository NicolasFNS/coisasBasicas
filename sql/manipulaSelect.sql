SELECT 
  REPEAT(name, 3) AS name,
  REVERSE(characteristics) AS characteristics
FROM monsters

/* o desafio eraa fazer um select onde o nome é a string original repetida três vezes (não adicione espaços)
e as características são as strings originais ao contrário (por exemplo, 'abc, def, ghi' se torna 'ihg,fed,cba')*/
